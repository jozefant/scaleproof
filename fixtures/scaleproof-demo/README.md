# Demo Startup API

Install dependencies and run the service:

```bash
npm install
npm run dev
```

The API stores customer accounts in PostgreSQL. This repository is intentionally
incomplete and exists only as a deterministic Scaleproof demonstration.
